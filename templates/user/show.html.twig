{% extends 'base.html.twig' %}

{% block title %}User{% endblock %}

{% block body %}
    <h1>User</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                 <td>{{ user.getId() }}</td>
            </tr>
            <tr>
                <th>Login</th>
                <td>{{ user.getUsername() }}</td>
            </tr>
          
        </tbody>
    </table>

    <a href="{{ path('user_index') }}"> <button class="btn btn-primary">retour</button></a>

    <a href="{{ path('user_edit', {'id': user.id}) }}"><button class="btn btn-primary">Modifier</button></a>

    {{ include('user/_delete_form.html.twig') }}
{% endblock %}
